pipeline {
    agent any

    stages {

        stage('Checkout Repo') {
            steps {
                checkout scm
            }
        }

        stage('Deploy Containers') {
            steps {
                sh '''
                  docker compose pull
                  docker compose up -d
                '''
            }
        }
    }
}
